{% extends 'base.html.twig' %}

{% block title %}Notesmots{% endblock %}

{% block body %}
    <h1>Notesmots</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ notesmot.id }}</td>
            </tr>
            <tr>
                <th>Mot</th>
                <td>{{ notesmot.mot }}</td>
            </tr>
            <tr>
                <th>Notes</th>
                <td>{{ notesmot.notes }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_notess_mots_index') }}">back to list</a>

    <a href="{{ path('app_notess_mots_edit', {'id': notesmot.id}) }}">edit</a>

    {{ include('notess_mots/_delete_form.html.twig') }}
{% endblock %}
